import NavigationComponent from "./NavigationComponent";

export default function HeaderComponent() {
    return (
        <header class="header">
            <NavigationComponent />
        </header>
    );
}
